#include <stdio.h>

int main() {
    char abc;
    scanf("%c",&abc);
    if(abc>=a && abc<=z){
        printf("Lowercase");
    }
    else{
        printf("Uppercase");
    }
    return 0;
}